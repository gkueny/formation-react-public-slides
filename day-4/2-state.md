#### State

```jsx
import React, { useState } from "react";

function ComponentWithState() {
  // Ici on déclare deux variables
  // count => C'est notre **state**
  // setCount => Fonction qui permet de mettre à jour count
  const [count, setCount] = useState(0);

  return <div>HERE</div>;
}
```
