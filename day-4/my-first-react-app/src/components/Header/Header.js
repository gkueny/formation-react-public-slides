import React from "react";

const Header = props => {
  console.log("Headetr");
  return (
    <header>
      <nav>
        <a href="#">Accueil</a>
      </nav>
    </header>
  );
};

export default Header;
